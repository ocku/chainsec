[project]
name = "fixture-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.32,<3",
  "importlib-metadata; python_version < '3.12'",
  "acme-client[crypto] @ https://packages.example.test/acme-client-1.0.tar.gz",
]

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.27"
pendulum = { version = ">=3,<4", optional = true }
